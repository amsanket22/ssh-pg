# ssh-pg

## Boilerplate to connect PostgreSql database that is behind the SSH

### Instructions

`npm i`

`cp .env.sample .env`

provide your ssh and db credentials

and then run

`node index.js`

Sample query has been added to demonstrate, you can modify the script as per your requirement
